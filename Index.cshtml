

@model IEnumerable<SanaatanGroup.Models.JammuTechnologyPartner>
@using SanaatanGroup.Models;
@{
    ViewBag.Title = "Team Sanaatan";
    ApplicationDbContext db = new ApplicationDbContext();
    var a = db._JammuTechnologyPartner.OrderBy(m => m.Name).ToList();
}

@*<div class="page-title title-1">
    <div class="container">
        <div class="row">
            <div class="cell-12">
                <h1 class="fx" data-animate="fadeInLeft">Meet <span>the team</span></h1>

            </div>
        </div>
    </div>
</div>

<div class="sectionWrapper">
    <div class="container">
        <div class="team-boxes-2">

            <div class="row">
                @foreach (var item in a)
                {
                    <div class="cell-3 fx" data-animate="fadeInDown">

                        <div class="team-box-2">
                            <div class="team-img">
                                <img alt="" src="~/Content/UploadedImages/Jammu/@item.Image">
                            </div>
                            <div class="team-details">
                                <h3>@item.Name</h3>
                                <div class="t-position">@item.Designation</div>
                                <p>@item.KeySkills</p>

                            </div>
                        </div>
                    </div>
                }

            </div>
        </div>
    </div>
</div>*@



<div class="page-title title-1">
    <div class="container">
        <div class="row">
            <div class="cell-12">
                <h1 class="fx" data-animate="fadeInLeft">Meet <span>the team</span></h1>

            </div>
        </div>
    </div>
</div>

<div class="row">
@foreach (var item in a)
{
    <div class="cell-2 fx" data-animate="fadeInDown">
        <div class="team-box">
            <div class="team-img">
                <img alt="" src="~/Content/UploadedImages/Jammu/@item.Image" height="50" width="50">
                <h3 style="color:black">@item.Name</h3>
                <center>  <span style="color:maroon;text-align:center">@item.Designation</span></center>
            </div>
           
        </div>
    </div>
}
    </div>
<br />